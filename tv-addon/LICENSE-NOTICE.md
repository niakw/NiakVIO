# Source policy

This addon does not host, proxy, cache, restream or redistribute television content.

Only channels marked as verified and linked to an official publisher page are eligible. Entries matching `blacklist.config.json` are rejected from catalog, metadata and stream responses.

When an official reusable direct stream is unavailable, the addon returns the publisher's official live page as an external URL.
