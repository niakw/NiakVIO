# 5.18.27

- Frenchstream : domaine courant validé via le hub fstream.org et récupération des lecteurs depuis la fiche lorsque l’ancienne route film_api n’est plus disponible.
- StreamZo : prise en charge des attributs `data-embed`, `data-src`, `data-player` et `data-url` utilisés par les pages actuelles.
- Ajout de `provider-hubs.json` comme registre documentaire des hubs et domaines vérifiés.
- Aucun fallback propre au banc de test local n’est publié comme résultat natif.
