## 2026-09-07 — Route recognition v6 portfolio repair started

- Public release remains 5.21.37; the previous 5.21.38 promotion is frozen because route/runtime yield is still materially incomplete.
- Dedicated repair branch: `workbench/route-recognition-v6`, based on main commit `e7e4e6f0f97eb2402dbc159bb3981d1fd39b3e10`.
- First branch fix commit: `68ddbb4da9d8afa44d7db795787b434c4b99fcf4` (`fix: preserve Request bodies in route proof tracing`). The route-proof upgrader now captures bodies carried by `fetch(Request)` via `await input.clone().text()` without consuming the provider request. This targets providers such as AnimeZey where the upstream POST was observed but the replayable request DATA was lost.
- User explicitly requires maximum provider recovery, not only the named VO examples. Repair scope is all currently non-working/unrepaired providers.
- Providers already proven functional/corrected and therefore excluded from repeated portfolio repair probes: `allwish`, `anime-sama`, `castle`, `hindmoviez`, `kehflix`, `neko-sama`, `streamzo`, `videasy`, `wookafr`. They remain protected by global regression/static/runtime gates but must not be needlessly re-run through route recognition/yield while repairing others.
- Historical VO sentinels to recover/check include MoviesHunt, ZinkMovies, VegaMovies, PersianStremio, 4KHDHubNew and AnimeZey; HindMoviez is already functional and is in the skip set.
- Confirmed systemic defects from the accepted route-proof evidence: MoviesHunt upstream returns streams but reconstructed runtime can lose them after following a slow/dead external player root; AnimeZey proved a POST search route yet reconstruction retained an incorrect route due incomplete request-body proof; some providers have live-proven routes but no executable recipe/plan; domain errors such as ZinkMovies 451 and stale API subdomains must remain separate from recognition/runtime defects.
- Required architecture change: one canonical `recognition -> proof DATA -> correction/materialization -> targeted post-repair validation` pipeline must be reused by the current repair workflow, LEARN and an explicit FORCE workflow. Learn/Force must not use the older ineffective repair path. The pipeline must support a persisted skip list of already-green providers and a hard `upstream-positive -> reconstructed-positive` regression gate for targeted providers.
- Next repair priorities are common-family fixes rather than provider-specific hacks: proof full Request method/headers/body; preserve POST/API request specs; reject useless slow external root branches from generic crawler traversal; ensure proven request DATA wins over stale runtime routes; run recognition and quick-yield only for unresolved providers; checkpoint every result to MEMORY.md.
