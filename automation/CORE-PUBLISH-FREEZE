# NiakVIO Core publication freeze

Temporary safety marker used while stale GitHub Actions generations are being drained.
When present, Core finalizers must fail closed before any publication to main.
Remove only after queued/in-progress stale runs are gone and the authoritative final validation is ready to run.
