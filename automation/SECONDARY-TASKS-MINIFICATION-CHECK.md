# Minification verification checkpoint

This file records that JavaScript minimization/minification is a mandatory secondary task. Verification must cover Provider v3 minimizer usage, no Terser production path, managed marker preservation, deterministic byte output/fixed point, manifest filename/hash synchronization, and runtime-semantic equivalence after minimization. It should be folded into MEMORY.md and removed or retained only if useful after the final checkpoint.
