/* NUVIO_ADAPTIVE_DOMAIN_RECOVERY_V1:BEGIN */

/* NUVIO_PROVIDER_SECURITY_HARDENING_V1:dd4a97437ea7 */
function __nuvioHostMatches(value,expected){
  try{
    var raw=String(value==null?"":value).trim();
    var wanted=String(expected==null?"":expected).toLowerCase().replace(/^\.+|\.+$/g,"");
    if(!raw||!wanted)return false;
    var parsed=new URL(/^[a-z][a-z0-9+.-]*:\/\//i.test(raw)?raw:"https://"+raw);
    var host=String(parsed.hostname||"").toLowerCase().replace(/\.$/,"");
    return host===wanted||host.endsWith("."+wanted);
  }catch(_error){return false}
}
var __nuvioProviderSilentLog=function(){};
;(function(g,encoded){
  if(!g||typeof g.fetch!=="function"||g.__nuvioAdaptiveDomainRecoveryV1)return;
  var nativeFetch=g.fetch.bind(g), groups=[];
  try{
    var decoded=JSON.parse(typeof atob==="function"?atob(encoded):Buffer.from(encoded,"base64").toString("utf8"));
    groups=Array.isArray(decoded)?decoded:(decoded&&Array.isArray(decoded.groups)?decoded.groups:[]);
  }catch(_e){return;}
  var cache=Object.create(null);
  function obsolete(status){return status===403||status===404||status===408||status===410||status===425||status===429||status===451||status===500||status===502||status===503||status===504||(status>=520&&status<=524);}
  function groupFor(host){
    host=String(host||"").toLowerCase();
    for(var i=0;i<groups.length;i++)if(groups[i].hosts.indexOf(host)!==-1)return groups[i];
    return null;
  }
  function rebuild(raw,origin){
    var source=new URL(raw), target=new URL(origin);
    target.pathname=source.pathname; target.search=source.search; target.hash=source.hash;
    return target.toString();
  }
  function cloneInput(input,url){
    try{return typeof Request!=="undefined"&&input instanceof Request?new Request(url,input):url;}catch(_e){return url;}
  }
  function attempt(input,init,raw,group,index){
    if(index>=group.candidates.length)return nativeFetch(input,init);
    var origin=group.candidates[index], url;
    try{url=rebuild(raw,origin);}catch(_e){return attempt(input,init,raw,group,index+1);}
    return nativeFetch(cloneInput(input,url),init).then(function(response){
      if(response&&!obsolete(response.status)){
        try{cache[new URL(raw).hostname.toLowerCase()]=origin;}catch(_e){}
        return response;
      }
      return attempt(input,init,raw,group,index+1);
    },function(){return attempt(input,init,raw,group,index+1);});
  }
  g.fetch=function(input,init){
    var raw;
    try{raw=typeof Request!=="undefined"&&input instanceof Request?input.url:String(input);}catch(_e){return nativeFetch(input,init);}
    var parsed, group;
    try{parsed=new URL(raw);group=groupFor(parsed.hostname);}catch(_e){return nativeFetch(input,init);}
    if(!group)return nativeFetch(input,init);
    var remembered=cache[parsed.hostname.toLowerCase()];
    if(remembered){
      var preferred=[remembered], rest=[];
      for(var i=0;i<group.candidates.length;i++)if(group.candidates[i]!==remembered)rest.push(group.candidates[i]);
      group={hosts:group.hosts,candidates:preferred.concat(rest)};
    }
    return attempt(input,init,raw,group,0);
  };
  g.__nuvioAdaptiveDomainRecoveryV1=true;
})(typeof globalThis!=="undefined"?globalThis:this,"");
/* NUVIO_ADAPTIVE_DOMAIN_RECOVERY_V1:END */
const TMDB_API_KEY="439c478a771f35c05022f9feabcca01c";const TMDB_BASE_URL="https://api.themoviedb.org/3";const VIDNEST_BASE_URL="https://first.vidnest.fun";const VIDNEST_PROXY_URL="https://vidnest.animanga.fun/proxy";const PASSPHRASE="A7kP9mQeXU2BWcD4fRZV+Sg8yN0/M5tLbC1HJQwYe6o=";const SERVERS=["hollymoviehd","primesrc","ophim","flixhq","vidlink","rogflix"];const WORKING_HEADERS={accept:"*/*","accept-encoding":"gzip, deflate, br, zstd","accept-language":"en-GB,en-US;q=0.9,en;q=0.8",origin:"https://vidnest.fun",referer:"https://vidnest.fun/","sec-ch-ua":'"Google Chrome";v="143", "Chromium";v="143", "Not A(Brand";v="24"',"sec-ch-ua-mobile":"?1","sec-ch-ua-platform":'"Android"',"sec-fetch-dest":"empty","sec-fetch-mode":"cors","sec-fetch-site":"same-site","user-agent":"Mozilla/5.0 (Linux; Android 6.0; Nexus 5 Build/MRA58N) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/143.0.0.0 Mobile Safari/537.36"};const PLAYBACK_HEADERS={"User-Agent":"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/137.0.0.0 Safari/537.36",Accept:"video/webm,video/ogg,video/*;q=0.9,application/ogg;q=0.7,audio/*;q=0.6,*/*;q=0.5","Accept-Language":"en-US,en;q=0.9","Accept-Encoding":"identity",Connection:"keep-alive","Sec-Fetch-Dest":"video","Sec-Fetch-Mode":"no-cors","Sec-Fetch-Site":"cross-site",DNT:"1"};const BASE64_CHARS="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";function base64ToBytes(base64){if(!base64)return new Uint8Array(0);let input=String(base64).replace(/=+$/,"");let output="";let bc=0,bs,buffer,idx=0;while(buffer=input.charAt(idx++)){buffer=BASE64_CHARS.indexOf(buffer);if(~buffer){bs=bc%4?bs*64+buffer:buffer;if(bc++%4){output+=String.fromCharCode(255&bs>>(-2*bc&6))}}}const bytes=new Uint8Array(output.length);for(let i=0;i<output.length;i++){bytes[i]=output.charCodeAt(i)}return bytes}function bytesToBase64(bytes){if(!bytes||bytes.length===0)return"";let output="";let i=0;const len=bytes.length;while(i<len){const a=bytes[i++];const b=i<len?bytes[i++]:0;const c=i<len?bytes[i++]:0;const bitmap=a<<16|b<<8|c;output+=BASE64_CHARS.charAt(bitmap>>18&63);output+=BASE64_CHARS.charAt(bitmap>>12&63);output+=i-2<len?BASE64_CHARS.charAt(bitmap>>6&63):"=";output+=i-1<len?BASE64_CHARS.charAt(bitmap&63):"="}return output}function atob(str){return base64ToBytes(str).map(byte=>String.fromCharCode(byte)).join("")}function decryptAesGcm(encryptedB64,passphraseB64){__nuvioProviderSilentLog("[Vidnest] Starting AES-GCM decryption via server...");const decryptServerUrl=process.env.DECRYPT_SERVER_URL||"https://aesdec.nuvioapp.space/decrypt";return fetch(decryptServerUrl,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({encryptedData:encryptedB64,passphrase:passphraseB64})}).then(response=>response.json()).then(data=>{if(data.error)throw new Error(data.error);__nuvioProviderSilentLog("[Vidnest] Server decryption successful");return data.decrypted}).catch(error=>{__nuvioProviderSilentLog(`[Vidnest] Server decryption failed: ${error.message}`);throw error})}function validateStreamUrl(url,headers){__nuvioProviderSilentLog(`[Vidnest] Validating stream URL: ${url.substring(0,60)}...`);if(url.includes(".m3u8")||url.includes("/streamsvr/")||url.includes("/stream2/")){__nuvioProviderSilentLog(`[Vidnest] Skipping validation for HLS/protected stream`);return Promise.resolve(true)}return fetch(url,{method:"HEAD",headers,timeout:5e3}).then(response=>{const isValid=response.ok||response.status===206||response.status===302||response.status===403;__nuvioProviderSilentLog(`[Vidnest] URL validation result: ${response.status} - ${isValid?"VALID":"INVALID"}`);return isValid}).catch(error=>{__nuvioProviderSilentLog(`[Vidnest] URL validation failed: ${error.message}`);if(url.includes(".m3u8")||url.includes("/streamsvr/")||url.includes("/stream2/")){return true}return false})}function makeRequest(url,options={}){const defaultHeaders={...WORKING_HEADERS};return fetch(url,{method:options.method||"GET",headers:{...defaultHeaders,...options.headers},...options}).then(function(response){if(!response.ok){throw new Error(`HTTP ${response.status}: ${response.statusText}`)}return response}).catch(function(error){__nuvioProviderSilentLog(`[Vidnest] Request failed for ${url}: ${error.message}`);throw error})}function getTMDBDetails(tmdbId,mediaType){const endpoint=mediaType==="tv"?"tv":"movie";const url=`${TMDB_BASE_URL}/${endpoint}/${tmdbId}?api_key=${TMDB_API_KEY}&append_to_response=external_ids`;return makeRequest(url).then(function(response){return response.json()}).then(function(data){const title=mediaType==="tv"?data.name:data.title;const releaseDate=mediaType==="tv"?data.first_air_date:data.release_date;const year=releaseDate?parseInt(releaseDate.split("-")[0]):null;return{title,year,imdbId:data.external_ids?.imdb_id||null}})}function extractQuality(url){if(!url)return"Unknown";const qualityPatterns=[/(\d{3,4})p/i,/(\d{3,4})k/i,/quality[_-]?(\d{3,4})/i,/res[_-]?(\d{3,4})/i,/(\d{3,4})x\d{3,4}/i];for(const pattern of qualityPatterns){const match=url.match(pattern);if(match){const qualityNum=parseInt(match[1]);if(qualityNum>=240&&qualityNum<=4320){return`${qualityNum}p`}}}if(url.includes("1080")||url.includes("1920"))return"1080p";if(url.includes("720")||url.includes("1280"))return"720p";if(url.includes("480")||url.includes("854"))return"480p";if(url.includes("360")||url.includes("640"))return"360p";if(url.includes("240")||url.includes("426"))return"240p";return"Unknown"}function wrapUrlWithProxy(url){if(__nuvioHostMatches(url,"flashstream.cc")||url.includes("streamsvr/")||url.includes("/pl/")||url.includes("rogflix")||__nuvioHostMatches(url,"lethe399key.com")&&url.includes("/stream2/")){let origin="https://flashstream.cc";let referer="https://flashstream.cc/";if(__nuvioHostMatches(url,"lethe399key.com")){origin="https://lethe399key.com";referer="https://lethe399key.com/"}const proxyHeaders={"user-agent":"Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:137.0) Gecko/20100101 Firefox/137.0",accept:"*/*","accept-language":"en-US,en;q=0.5","sec-fetch-dest":"empty","sec-fetch-mode":"cors","sec-fetch-site":"cross-site",origin,referer};const encodedUrl=encodeURIComponent(url);const encodedHeaders=encodeURIComponent(JSON.stringify(proxyHeaders));const proxiedUrl=`${VIDNEST_PROXY_URL}?url=${encodedUrl}&headers=${encodedHeaders}`;__nuvioProviderSilentLog(`[Vidnest] Wrapping URL through proxy: ${url.substring(0,60)}...`);return proxiedUrl}return url}function processVidnestResponse(data,serverName,mediaInfo,seasonNum,episodeNum){const streams=[];try{__nuvioProviderSilentLog(`[Vidnest] Processing response from ${serverName}:`,JSON.stringify(data,null,2));let sources=[];if(data.sources&&Array.isArray(data.sources)){sources=data.sources}else if(data.streams&&Array.isArray(data.streams)){sources=data.streams}else if(data.url&&typeof data.url==="string"){sources=[{url:data.url,type:"hls",headers:data.headers,subtitles:data.subtitles}]}else if(data.data&&typeof data.data==="string"){sources=[{url:data.data,type:"hls",headers:data.headers,provider:data.provider}]}else if(data.success&&(data.sources||data.streams)){sources=data.sources||data.streams||[]}if(!Array.isArray(sources)||sources.length===0){__nuvioProviderSilentLog(`[Vidnest] ${serverName}: No sources/streams found in response`);return streams}sources.forEach((source,index)=>{if(!source)return;let videoUrl=source.file||source.url||source.src||source.link;if(!videoUrl){__nuvioProviderSilentLog(`[Vidnest] ${serverName}: Source ${index} has no video URL`);return}videoUrl=wrapUrlWithProxy(videoUrl);let languageInfo="";if(source.language){languageInfo=` [${source.language}]`}let labelInfo="";if(source.label){labelInfo=` - ${source.label}`}else if(source.source){labelInfo=` - ${source.source}`}let mediaTitle=mediaInfo.title||"Unknown";if(mediaInfo.year){mediaTitle+=` (${mediaInfo.year})`}if(seasonNum&&episodeNum){mediaTitle=`${mediaInfo.title} S${String(seasonNum).padStart(2,"0")}E${String(episodeNum).padStart(2,"0")}`}const quality="auto";streams.push({name:`Vidnest ${serverName.charAt(0).toUpperCase()+serverName.slice(1)}${labelInfo}${languageInfo}`,title:mediaTitle,url:videoUrl,quality,size:"Unknown",provider:"vidnest"});__nuvioProviderSilentLog(`[Vidnest] ${serverName}: Added ${quality}${languageInfo} stream: ${videoUrl.substring(0,60)}...`)})}catch(error){__nuvioProviderSilentLog(`[Vidnest] Error processing ${serverName} response: ${error.message}`)}return streams}function fetchFromServer(serverName,mediaType,tmdbId,mediaInfo,seasonNum,episodeNum){__nuvioProviderSilentLog(`[Vidnest] Fetching from ${serverName}...`);let apiUrl;if(mediaType==="tv"&&seasonNum&&episodeNum){apiUrl=`${VIDNEST_BASE_URL}/${serverName}/${mediaType}/${tmdbId}/${seasonNum}/${episodeNum}`}else{apiUrl=`${VIDNEST_BASE_URL}/${serverName}/${mediaType}/${tmdbId}`}if(serverName==="flixhq"){apiUrl+="?server=upcloud"}__nuvioProviderSilentLog(`[Vidnest] ${serverName} API URL: ${apiUrl}`);return makeRequest(apiUrl).then(function(response){return response.text()}).then(function(responseText){__nuvioProviderSilentLog(`[Vidnest] ${serverName} response length: ${responseText.length} characters`);try{const data=JSON.parse(responseText);if(data.encrypted&&data.data){__nuvioProviderSilentLog(`[Vidnest] ${serverName}: Detected encrypted response, decrypting...`);return decryptAesGcm(data.data,PASSPHRASE).then(function(decryptedText){__nuvioProviderSilentLog(`[Vidnest] ${serverName}: Decryption successful`);try{const decryptedData=JSON.parse(decryptedText);return processVidnestResponse(decryptedData,serverName,mediaInfo,seasonNum,episodeNum)}catch(parseError){__nuvioProviderSilentLog(`[Vidnest] ${serverName}: JSON parse error after decryption: ${parseError.message}`);return[]}})}else{return processVidnestResponse(data,serverName,mediaInfo,seasonNum,episodeNum)}}catch(parseError){__nuvioProviderSilentLog(`[Vidnest] ${serverName}: Invalid JSON response: ${parseError.message}`);return[]}}).catch(function(error){__nuvioProviderSilentLog(`[Vidnest] ${serverName} error: ${error.message}`);return[]})}function getStreams(tmdbId,mediaType,seasonNum,episodeNum){__nuvioProviderSilentLog(`[Vidnest] Starting extraction for TMDB ID: ${tmdbId}, Type: ${mediaType}${mediaType==="tv"?`, S:${seasonNum}E:${episodeNum}`:""}`);return new Promise((resolve,reject)=>{getTMDBDetails(tmdbId,mediaType).then(function(mediaInfo){__nuvioProviderSilentLog(`[Vidnest] TMDB Info: "${mediaInfo.title}" (${mediaInfo.year||"N/A"})`);const serverPromises=SERVERS.map(serverName=>fetchFromServer(serverName,mediaType,tmdbId,mediaInfo,seasonNum,episodeNum));return Promise.all(serverPromises).then(function(results){const allStreams=[];results.forEach(streams=>{allStreams.push(...streams)});const uniqueStreams=[];const seenUrls=new Set;allStreams.forEach(stream=>{if(!seenUrls.has(stream.url)){seenUrls.add(stream.url);uniqueStreams.push(stream)}});__nuvioProviderSilentLog(`[Vidnest] Validating ${uniqueStreams.length} streams...`);const validationPromises=uniqueStreams.map(stream=>validateStreamUrl(stream.url,PLAYBACK_HEADERS).then(isValid=>({stream,isValid})));return Promise.all(validationPromises).then(function(results){const validStreams=results.filter(r=>r.isValid).map(r=>r.stream);__nuvioProviderSilentLog(`[Vidnest] Filtered ${uniqueStreams.length-validStreams.length} broken links`);const getQualityValue=quality=>{const q=quality.toLowerCase().replace(/p$/,"");if(q==="4k"||q==="2160")return 2160;if(q==="1440")return 1440;if(q==="1080")return 1080;if(q==="720")return 720;if(q==="480")return 480;if(q==="360")return 360;if(q==="240")return 240;if(q==="unknown")return 0;const numQuality=parseInt(q);if(!isNaN(numQuality)&&numQuality>0){return numQuality}return 1};validStreams.sort((a,b)=>{const qualityA=getQualityValue(a.quality);const qualityB=getQualityValue(b.quality);return qualityB-qualityA});__nuvioProviderSilentLog(`[Vidnest] Total valid streams found: ${validStreams.length}`);resolve(validStreams)})})}).catch(function(error){__nuvioProviderSilentLog(`[Vidnest] Error fetching media details: ${error.message}`);resolve([])})})}if(typeof module!=="undefined"&&module.exports){module.exports={getStreams}}else{global.getStreams=getStreams}