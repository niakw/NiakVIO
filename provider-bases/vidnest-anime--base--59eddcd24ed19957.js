/* NUVIO_PROVIDER_SECURITY_HARDENING_V1:428964b9cb8a */
var __nuvioProviderSilentLog=function(){};const VIDNEST_BASE_URL="https://backend.vidnest.fun";const PASSPHRASE="A7kP9mQeXU2BWcD4fRZV+Sg8yN0/M5tLbC1HJQwYe6o=";const TMDB_API_KEY="439c478a771f35c05022f9feabcca01c";const TMDB_BASE_URL="https://api.themoviedb.org/3";const ANIME_SERVERS={hindi:{url:(id,ep)=>`${VIDNEST_BASE_URL}/animeworld/${id}/${ep}/server/my%20server`,language:"Hindi",needsDecryption:true},satoru:{url:(id,ep)=>`${VIDNEST_BASE_URL}/satoru/${id}/${ep}`,language:"Original",needsDecryption:true},miko:{url:(id,ep,lang)=>`${VIDNEST_BASE_URL}/aniwave/${id}/${ep}/${lang}/wave`,language:"Original",needsDecryption:true,supportsSubDub:true},pahe:{url:(id,ep,lang)=>`${VIDNEST_BASE_URL}/aniwave/${id}/${ep}/${lang}/pahe`,language:"Original",needsDecryption:true,supportsSubDub:true},anya:{url:(id,ep,lang)=>`${VIDNEST_BASE_URL}/aniwave/${id}/${ep}/${lang}/anya`,language:"Original",needsDecryption:true,supportsSubDub:true}};const WORKING_HEADERS={"User-Agent":"Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/137.0.0.0 Mobile Safari/537.36",Accept:"application/json, text/plain, */*","Accept-Language":"en-US,en;q=0.9","Accept-Encoding":"gzip, deflate, br",Referer:"https://vidnest.fun/",Origin:"https://vidnest.fun",DNT:"1"};const BASE64_CHARS="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";function base64ToBytes(base64){if(!base64)return new Uint8Array(0);let input=String(base64).replace(/=+$/,"");let output="";let bc=0,bs,buffer,idx=0;while(buffer=input.charAt(idx++)){buffer=BASE64_CHARS.indexOf(buffer);if(~buffer){bs=bc%4?bs*64+buffer:buffer;if(bc++%4){output+=String.fromCharCode(255&bs>>(-2*bc&6))}}}const bytes=new Uint8Array(output.length);for(let i=0;i<output.length;i++){bytes[i]=output.charCodeAt(i)}return bytes}function bytesToBase64(bytes){if(!bytes||bytes.length===0)return"";let output="";let i=0;const len=bytes.length;while(i<len){const a=bytes[i++];const b=i<len?bytes[i++]:0;const c=i<len?bytes[i++]:0;const bitmap=a<<16|b<<8|c;output+=BASE64_CHARS.charAt(bitmap>>18&63);output+=BASE64_CHARS.charAt(bitmap>>12&63);output+=i-2<len?BASE64_CHARS.charAt(bitmap>>6&63):"=";output+=i-1<len?BASE64_CHARS.charAt(bitmap&63):"="}return output}function atob(str){return base64ToBytes(str).map(byte=>String.fromCharCode(byte)).join("")}function decryptAesGcm(encryptedB64,passphraseB64){__nuvioProviderSilentLog("[VidnestAnime] Starting AES-GCM decryption via server...");return fetch("https://aesdec.nuvioapp.space/decrypt",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({encryptedData:encryptedB64,passphrase:passphraseB64})}).then(response=>response.json()).then(data=>{if(data.error)throw new Error(data.error);__nuvioProviderSilentLog("[VidnestAnime] Server decryption successful");return data.decrypted}).catch(error=>{__nuvioProviderSilentLog(`[VidnestAnime] Server decryption failed: ${error.message}`);throw error})}function makeRequest(url,options={}){const defaultHeaders={...WORKING_HEADERS};return fetch(url,{method:options.method||"GET",headers:{...defaultHeaders,...options.headers},...options}).then(function(response){if(!response.ok){throw new Error(`HTTP ${response.status}: ${response.statusText}`)}return response}).catch(function(error){__nuvioProviderSilentLog(`[VidnestAnime] Request failed for ${url}: ${error.message}`);throw error})}function getTMDBDetails(tmdbId,mediaType){const endpoint=mediaType==="tv"?"tv":"movie";const url=`${TMDB_BASE_URL}/${endpoint}/${tmdbId}?api_key=${TMDB_API_KEY}`;return makeRequest(url).then(function(response){return response.json()}).then(function(data){const title=mediaType==="tv"?data.name:data.title;const releaseDate=mediaType==="tv"?data.first_air_date:data.release_date;const year=releaseDate?parseInt(releaseDate.split("-")[0]):null;return{title,year}})}function mapTMDBToAniList(tmdbId,title,year){__nuvioProviderSilentLog(`[VidnestAnime] Mapping TMDB ${tmdbId} to AniList...`);const query=`\n        query ($search: String, $year: Int) {\n            Media(search: $search, seasonYear: $year, type: ANIME, format_in: [TV, TV_SHORT, MOVIE, OVA, ONA, SPECIAL]) {\n                id\n                title {\n                    romaji\n                    english\n                    native\n                }\n                seasonYear\n            }\n        }\n    `;return fetch("https://graphql.anilist.co",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify({query,variables:{search:title,year}})}).then(function(response){return response.json()}).then(function(data){if(data.data&&data.data.Media){const anilistId=data.data.Media.id;__nuvioProviderSilentLog(`[VidnestAnime] Mapped to AniList ID: ${anilistId} (${data.data.Media.title.english||data.data.Media.title.romaji})`);return anilistId}throw new Error(`No AniList mapping found for "${title}" (${year})`)})}function getTMDBSeasonEpisodeCounts(tmdbId,targetSeason){__nuvioProviderSilentLog(`[VidnestAnime] Fetching season info for TMDB ${tmdbId}, seasons 1-${targetSeason}`);const seasonPromises=[];for(let s=1;s<targetSeason;s++){const url=`${TMDB_BASE_URL}/tv/${tmdbId}/season/${s}?api_key=${TMDB_API_KEY}`;seasonPromises.push(makeRequest(url).then(function(response){return response.json()}).then(function(data){return data.episodes?data.episodes.length:0}).catch(function(error){__nuvioProviderSilentLog(`[VidnestAnime] Failed to fetch season ${s}: ${error.message}`);return 0}))}return Promise.all(seasonPromises).then(function(episodeCounts){const totalPreviousEpisodes=episodeCounts.reduce((sum,count)=>sum+count,0);__nuvioProviderSilentLog(`[VidnestAnime] Previous seasons episode counts: ${episodeCounts.join(", ")} = ${totalPreviousEpisodes} total`);return totalPreviousEpisodes})}function getAnimeMetadata(anilistId,episodeNum){__nuvioProviderSilentLog(`[VidnestAnime] Fetching metadata for AniList ID: ${anilistId}, Episode: ${episodeNum}`);return makeRequest(`https://api.ani.zip/mappings?anilist_id=${anilistId}`).then(function(response){return response.json()}).then(function(data){const episode=data.episodes?.[String(episodeNum)]||null;return{anilistId,title:data.title?.english||data.titles?.en||`Anime ID: ${anilistId}`,episodeTitle:episode?.title||null,poster:episode?.image||data.images?.find(i=>i.coverType==="Poster")?.url||"",year:data.year||null}}).catch(function(error){__nuvioProviderSilentLog(`[VidnestAnime] Failed to fetch anime metadata: ${error.message}`);return{anilistId,title:`Anime ID: ${anilistId}`,episodeTitle:null,poster:"",year:null}})}function fetchFromAnimeServer(serverName,serverConfig,anilistId,episodeNum,subDub){__nuvioProviderSilentLog(`[VidnestAnime] Fetching from ${serverName}...`);const url=serverConfig.supportsSubDub?serverConfig.url(anilistId,episodeNum,subDub||"sub"):serverConfig.url(anilistId,episodeNum);__nuvioProviderSilentLog(`[VidnestAnime] ${serverName} API URL: ${url}`);return makeRequest(url).then(function(response){return response.text()}).then(function(responseText){__nuvioProviderSilentLog(`[VidnestAnime] ${serverName} response length: ${responseText.length} characters`);try{const data=JSON.parse(responseText);if(serverConfig.needsDecryption&&data.encrypted&&data.data){__nuvioProviderSilentLog(`[VidnestAnime] ${serverName}: Detected encrypted response, decrypting...`);return decryptAesGcm(data.data,PASSPHRASE).then(function(decryptedText){__nuvioProviderSilentLog(`[VidnestAnime] ${serverName}: Decryption successful`);try{const decryptedData=JSON.parse(decryptedText);return processAnimeResponse(decryptedData,serverName,serverConfig,subDub)}catch(parseError){__nuvioProviderSilentLog(`[VidnestAnime] ${serverName}: JSON parse error after decryption: ${parseError.message}`);return[]}})}else{return processAnimeResponse(data,serverName,serverConfig,subDub)}}catch(parseError){__nuvioProviderSilentLog(`[VidnestAnime] ${serverName}: Invalid JSON response: ${parseError.message}`);return[]}}).catch(function(error){__nuvioProviderSilentLog(`[VidnestAnime] ${serverName} error: ${error.message}`);return[]})}function processAnimeResponse(data,serverName,serverConfig,subDub){const streams=[];try{__nuvioProviderSilentLog(`[VidnestAnime] Processing response from ${serverName}`);const sources=data.sources||data.streams||[];const subtitles=data.subtitles||[];const intro=data.intro||null;const outro=data.outro||null;if(!Array.isArray(sources)||sources.length===0){__nuvioProviderSilentLog(`[VidnestAnime] ${serverName}: No sources/streams array found`);return streams}let language=serverConfig.language;if(serverConfig.supportsSubDub&&subDub){if(subDub==="dub"){language="Dub"}else if(subDub==="sub"){language="Sub"}}sources.forEach((source,index)=>{if(!source)return;const videoUrl=source.file||source.url||source.src||source.link;if(!videoUrl){__nuvioProviderSilentLog(`[VidnestAnime] ${serverName}: Source ${index} has no video URL`);return}const processedSubtitles=subtitles.map(sub=>({file:sub.file||sub.url,kind:sub.kind||"subtitles",label:sub.label||sub.lang||"Unknown",default:sub.default||false}));const streamHeaders=serverName==="miko"&&source.headers?source.headers:WORKING_HEADERS;streams.push({name:`VidnestAnime ${serverName.charAt(0).toUpperCase()+serverName.slice(1)} [${language}] - Adaptive`,url:videoUrl,quality:"Adaptive",subtitles:processedSubtitles,intro,outro,headers:streamHeaders,provider:"vidnest-anime"});__nuvioProviderSilentLog(`[VidnestAnime] ${serverName}: Added ${language} stream with ${processedSubtitles.length} subtitles`);__nuvioProviderSilentLog(`[VidnestAnime] ${serverName}: Stream URL: ${videoUrl}`);__nuvioProviderSilentLog(`[VidnestAnime] ${serverName}: Complete Stream Object:`,JSON.stringify({name:`VidnestAnime ${serverName.charAt(0).toUpperCase()+serverName.slice(1)} [${language}] - Adaptive`,url:videoUrl,quality:"Adaptive",subtitles:processedSubtitles,intro,outro,headers:streamHeaders,provider:"vidnest-anime"},null,2))})}catch(error){__nuvioProviderSilentLog(`[VidnestAnime] Error processing ${serverName} response: ${error.message}`)}return streams}function getStreams(tmdbId,mediaType,seasonNum,episodeNum){__nuvioProviderSilentLog(`[VidnestAnime] Starting extraction for TMDB ID: ${tmdbId}, Type: ${mediaType}, S${seasonNum}E${episodeNum}`);return new Promise(function(resolve,reject){getTMDBDetails(tmdbId,mediaType).then(function(tmdbInfo){__nuvioProviderSilentLog(`[VidnestAnime] TMDB: "${tmdbInfo.title}" (${tmdbInfo.year})`);return mapTMDBToAniList(tmdbId,tmdbInfo.title,tmdbInfo.year)}).then(function(anilistId){const season=seasonNum||1;const episode=episodeNum||1;if(mediaType==="tv"&&season>1){return getTMDBSeasonEpisodeCounts(tmdbId,season).then(function(previousEpisodesCount){const absoluteEpisode=previousEpisodesCount+episode;__nuvioProviderSilentLog(`[VidnestAnime] Converted S${season}E${episode} → Absolute Episode ${absoluteEpisode}`);return{anilistId,absoluteEpisode}})}else{return{anilistId,absoluteEpisode:episode}}}).then(function(data){return getAnimeMetadata(data.anilistId,data.absoluteEpisode).then(function(metadata){metadata.anilistId=data.anilistId;metadata.absoluteEpisode=data.absoluteEpisode;return metadata})}).then(function(metadata){__nuvioProviderSilentLog(`[VidnestAnime] Anime: "${metadata.title}" - Episode ${metadata.absoluteEpisode}`);const serverPromises=[];Object.entries(ANIME_SERVERS).forEach(function([serverName,serverConfig]){if(serverConfig.supportsSubDub){serverPromises.push(fetchFromAnimeServer(serverName,serverConfig,metadata.anilistId,metadata.absoluteEpisode,"sub"));serverPromises.push(fetchFromAnimeServer(serverName,serverConfig,metadata.anilistId,metadata.absoluteEpisode,"dub"))}else{serverPromises.push(fetchFromAnimeServer(serverName,serverConfig,metadata.anilistId,metadata.absoluteEpisode,"sub"))}});return Promise.all(serverPromises).then(function(results){const allStreams=[];results.forEach(function(streams){allStreams.push(...streams)});allStreams.forEach(function(stream){stream.title=metadata.episodeTitle?`${metadata.title} - ${metadata.episodeTitle}`:`${metadata.title} - Episode ${metadata.absoluteEpisode}`;stream.poster=metadata.poster});const uniqueStreams=[];const seenUrls=new Set;allStreams.forEach(function(stream){if(!seenUrls.has(stream.url)){seenUrls.add(stream.url);uniqueStreams.push(stream)}});const sortedStreams=uniqueStreams.sort(function(a,b){const getPriority=function(stream){const name=stream.name.toLowerCase();if(name.includes("satoru")&&name.includes("original"))return 1;if(name.includes("hindi"))return 2;if(name.includes("[sub]"))return 3;if(name.includes("[dub]"))return 4;return 5};return getPriority(a)-getPriority(b)});__nuvioProviderSilentLog(`[VidnestAnime] Total streams found: ${sortedStreams.length}`);resolve(sortedStreams)})}).catch(function(error){__nuvioProviderSilentLog(`[VidnestAnime] Error: ${error.message}`);resolve([])})})}if(typeof module!=="undefined"&&module.exports){module.exports={getStreams}}else{global.getStreams=getStreams}