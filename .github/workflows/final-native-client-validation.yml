name: Legacy native validation wrapper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by final-native-client-validation-v2.yml"
