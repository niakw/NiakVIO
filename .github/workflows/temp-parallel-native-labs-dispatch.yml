name: TEMP - Manual Native Labs Dispatch

on:
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch native acceptance workflows on repair ref
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          REF: fix/labs-5.21.44-20260912
        shell: bash
        run: |
          set -euo pipefail
          for wf in \
            native-device-corpus-acceptance.yml \
            native-mobile-reader-acceptance.yml \
            native-desktop-reader-acceptance.yml
          do
            echo "Dispatching $wf on $REF"
            gh workflow run "$wf" --repo "$REPO" --ref "$REF"
          done
          echo "FIELD_PARALLEL_NATIVE_LABS_DISPATCHED=3"
