name: Permanent real-client labs

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/triggers/permanent-real-client-labs"

permissions:
  contents: read

concurrency:
  group: permanent-real-client-labs
  cancel-in-progress: false

env:
  NIAKVIO_DESKTOP_SHA: "8977fda639f5791947e6d8b36025635a77aed5da"
  NIAKVIO_MOBILE_SHA: "0d6e30e4566e6f9f2051d5f80a1e3f1e0fcb1f18"

jobs:
  desktop-real-runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - name: Checkout Niakvio
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: main
          path: niakvio
      - name: Set up JDK
        uses: actions/setup-java@dded0888837ed1f317902acf8a20df0ad188d165
        with:
          distribution: temurin
          java-version: '17'
      - name: Checkout exact official NuvioDesktop runtime
        shell: bash
        run: |
          git clone --filter=blob:none --no-checkout https://github.com/NuvioMedia/NuvioDesktop.git nuvio-desktop
          cd nuvio-desktop
          git checkout "$NIAKVIO_DESKTOP_SHA"
          touch local.properties
      - name: Inject exact Mon Ninja provider test
        shell: bash
        run: |
          mkdir -p nuvio-desktop/composeApp/src/desktopTest/kotlin/com/nuvio/app/features/plugins
          cat > nuvio-desktop/composeApp/src/desktopTest/kotlin/com/nuvio/app/features/plugins/NiakvioRealProviderDesktopTest.kt <<'KOTLIN'
          package com.nuvio.app.features.plugins

          import com.nuvio.app.features.plugins.runtime.PluginRuntime
          import java.io.File
          import kotlinx.coroutines.runBlocking
          import kotlin.test.Test
          import kotlin.test.assertEquals
          import kotlin.test.assertTrue

          class NiakvioRealProviderDesktopTest {
              private val root = File(System.getenv("GITHUB_WORKSPACE"), "niakvio")
              private val providers = listOf(
                  "moviebox" to "providers/moviebox--published-baseline--1c0c9c423a094e0d.js",
                  "netmirror" to "providers/netmirror--published-baseline--ccbd35984c20fc8b.js",
                  "streamzo" to "providers/streamzo--published-baseline--bc19a7586f9f3bb8.js",
              )

              @Test
              fun exactMonNinjaProviders() = runBlocking {
                  providers.forEach { (id, relative) ->
                      val started = System.currentTimeMillis()
                      val rows = PluginRuntime.executePlugin(
                          code = File(root, relative).readText(),
                          tmdbId = "1215638",
                          mediaType = "movie",
                          season = null,
                          episode = null,
                          scraperId = id,
                      )
                      println("FIELD_DESKTOP_RESULT provider=$id duration_ms=${System.currentTimeMillis()-started} count=${rows.size}")
                      rows.forEachIndexed { index, row ->
                          println("FIELD_DESKTOP_ROW provider=$id index=$index title=${row.title} name=${row.name} quality=${row.quality} language=${row.language} type=${row.type} url=${row.url} headers=${row.headers}")
                      }
                      if (id == "streamzo") {
                          assertTrue(rows.isNotEmpty(), "StreamZo must resolve Mon Ninja 3 in official desktop runtime")
                          assertTrue(rows.any { it.url.contains(".m3u8") }, "StreamZo must expose HLS on desktop")
                      }
                  }
              }

              @Test
              fun missingOptionalMetadataIsAccepted() = runBlocking {
                  val rows = PluginRuntime.executePlugin(
                      code = "module.exports.getStreams=async()=>[{title:'Metadata control',url:'https://example.test/video.mp4'}];",
                      tmdbId = "1215638",
                      mediaType = "movie",
                      season = null,
                      episode = null,
                      scraperId = "metadata-control",
                  )
                  assertEquals(1, rows.size)
                  assertEquals(null, rows.single().quality)
                  assertEquals(null, rows.single().language)
                  println("FIELD_DESKTOP_METADATA_NULL_ACCEPTED=true")
              }
          }
          KOTLIN
      - name: Execute official NuvioDesktop runtime test
        working-directory: nuvio-desktop
        shell: bash
        run: |
          ./gradlew :composeApp:desktopTest --tests 'com.nuvio.app.features.plugins.NiakvioRealProviderDesktopTest' --no-daemon --console=plain

  mobile-real-runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout Niakvio
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: main
          path: niakvio
      - name: Set up JDK
        uses: actions/setup-java@dded0888837ed1f317902acf8a20df0ad188d165
        with:
          distribution: temurin
          java-version: '17'
      - name: Checkout exact official NuvioMobile runtime
        shell: bash
        run: |
          git clone --filter=blob:none --no-checkout https://github.com/NuvioMedia/NuvioMobile.git nuvio-mobile
          cd nuvio-mobile
          git checkout "$NIAKVIO_MOBILE_SHA"
          touch local.properties
      - name: Inject exact Android Full host runtime test
        shell: bash
        run: |
          mkdir -p nuvio-mobile/composeApp/src/androidFullHostTest/kotlin/com/nuvio/app/features/plugins
          cat > nuvio-mobile/composeApp/src/androidFullHostTest/kotlin/com/nuvio/app/features/plugins/NiakvioRealProviderMobileTest.kt <<'KOTLIN'
          package com.nuvio.app.features.plugins

          import com.nuvio.app.features.plugins.runtime.PluginRuntime
          import java.io.File
          import kotlinx.coroutines.runBlocking
          import kotlin.test.Test
          import kotlin.test.assertEquals
          import kotlin.test.assertTrue

          class NiakvioRealProviderMobileTest {
              private val root = File(System.getenv("GITHUB_WORKSPACE"), "niakvio")
              private val providers = listOf(
                  "moviebox" to "providers/moviebox--published-baseline--1c0c9c423a094e0d.js",
                  "netmirror" to "providers/netmirror--published-baseline--ccbd35984c20fc8b.js",
                  "streamzo" to "providers/streamzo--published-baseline--bc19a7586f9f3bb8.js",
              )

              @Test
              fun exactMonNinjaProviders() = runBlocking {
                  providers.forEach { (id, relative) ->
                      val started = System.currentTimeMillis()
                      val rows = PluginRuntime.executePlugin(
                          code = File(root, relative).readText(),
                          tmdbId = "1215638",
                          mediaType = "movie",
                          season = null,
                          episode = null,
                          scraperId = id,
                      )
                      println("FIELD_MOBILE_RESULT provider=$id duration_ms=${System.currentTimeMillis()-started} count=${rows.size}")
                      rows.forEachIndexed { index, row ->
                          println("FIELD_MOBILE_ROW provider=$id index=$index title=${row.title} name=${row.name} quality=${row.quality} language=${row.language} type=${row.type} url=${row.url} headers=${row.headers}")
                      }
                      if (id == "streamzo") {
                          assertTrue(rows.isNotEmpty(), "StreamZo must resolve Mon Ninja 3 in official mobile Full runtime")
                          assertTrue(rows.any { it.url.contains(".m3u8") }, "StreamZo must expose HLS on mobile")
                      }
                  }
              }

              @Test
              fun missingOptionalMetadataIsAccepted() = runBlocking {
                  val rows = PluginRuntime.executePlugin(
                      code = "module.exports.getStreams=async()=>[{title:'Metadata control',url:'https://example.test/video.mp4'}];",
                      tmdbId = "1215638",
                      mediaType = "movie",
                      season = null,
                      episode = null,
                      scraperId = "metadata-control",
                  )
                  assertEquals(1, rows.size)
                  assertEquals(null, rows.single().quality)
                  assertEquals(null, rows.single().language)
                  println("FIELD_MOBILE_METADATA_NULL_ACCEPTED=true")
              }
          }
          KOTLIN
      - name: Execute official NuvioMobile Android Full host runtime test
        working-directory: nuvio-mobile
        shell: bash
        run: |
          TASK=$(./gradlew :composeApp:tasks --all -Pnuvio.android.distribution=full --console=plain | sed -n 's/^\([^ ]*[Aa]ndroid[^ ]*[Hh]ost[Tt]est[^ ]*\) .*/\1/p' | head -n1)
          if [ -z "$TASK" ]; then
            TASK=$(./gradlew :composeApp:tasks --all -Pnuvio.android.distribution=full --console=plain | awk '/androidHostTest/ {print $1; exit}')
          fi
          if [ -z "$TASK" ]; then
            echo "Unable to resolve Android host test task" >&2
            exit 1
          fi
          echo "Resolved mobile test task: $TASK"
          ./gradlew ":composeApp:$TASK" -Pnuvio.android.distribution=full --no-daemon --console=plain
