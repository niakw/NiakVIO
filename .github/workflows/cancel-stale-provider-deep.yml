name: Cancel stale provider deep run

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/cancel-stale-provider-deep.yml"

permissions:
  contents: read

concurrency:
  group: nuvio-provider-publish
  cancel-in-progress: true

jobs:
  cancel-stale-run:
    runs-on: ubuntu-latest
    steps:
      - name: Supersede stale deep run
        run: echo "Superseded stale provider deep run without starting provider probes."
